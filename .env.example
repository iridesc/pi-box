# pi-box 环境变量
# 网页访问密码（建议设置，用户名固定为 pi）
PI_WEB_PASSWORD=
# 反向代理（Caddy/Nginx）域名访问时信任的 Host 列表，逗号分隔。
# 不设置时仅接受 localhost / IP 直接访问，经域名反代访问会 403 "Untrusted request"。
# 例：PI_WEB_ALLOWED_HOSTS=pibox.irid.cc
PI_WEB_ALLOWED_HOSTS=
# 容器时区（影响 cron 执行时间）
TZ=Asia/Shanghai
